;;; init-evil.el --- Using VIM bindings ;; -*- lexical-bindings: t; -*-
;;; Commentary:
;;; Code:

(use-package evil
  :demand t
  :config
  (evil-mode 1))

(provide 'init-evil)
;;; init-evil.el ends here
